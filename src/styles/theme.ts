const fontSize = {
  xs: "0.5rem",
  sm: "0.75rem",
  base: "1rem",
  md: "1.25rem",
  lg: "1.5rem",
};

const colors = {
  primary: "#00ff6a",
  secondary: "#00b600",
};

const theme = {
  fontSize,
  colors,
};

export default theme;
