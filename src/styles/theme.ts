const fontSize = {
  xs: "0.5rem",
  sm: "0.75rem",
  base: "1rem",
  md: "1.25rem",
  lg: "1.5rem",
};

const colors = {
  primary: "#a7e6ff",
  secondary: "#23c1ff",
};

const theme = {
  fontSize,
  colors,
};

export default theme;
